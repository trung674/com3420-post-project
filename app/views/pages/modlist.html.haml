- content_for :title, "Map"

%h2 List of all current Moderators + Administrators

%p Below is a list of all currently assigned moderators and administrators for the website.

.row
  .col-md-6#contact-form
    %table.table.table-bordered
      %tr
        %th Email
        %th Account Created
        %th Last Logged in
        %th Is Active
      %tr
        - @mods.each do |mod|
          %td= mod.email
          %td= mod.created_at
          %td= mod.last_sign_in_at
          %td= mod.isActive

.row
  .col-md-6
    %a.btn.btn-primary{:href => "/modpanel", :role => "button"} Back
