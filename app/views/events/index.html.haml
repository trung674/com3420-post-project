.row
  .col-md-8.col-md-offset-1
    %h2 Upcoming Events

    #accordion.panel-group{"aria-multiselectable" => "true", :role => "tablist"}
      - @events.each do |event|
        .panel.panel-default
          #headingOne.panel-heading{:role => "tab"}
            %h4.panel-title
              %a{"aria-controls" => "event_#{event.id}", "aria-expanded" => "true", "data-parent" => "#accordion", "data-toggle" => "collapse", :href => "#event_#{event.id}", :role => "button"}
                = event.title
          .panel-collapse.collapse{:id => "event_#{event.id}", "aria-labelledby" => "heading", :role => "tabpanel"}
            .panel-body
              = event.description

      -# Code to be used later
      -#.panel.panel-info
      -#  .panel-heading
      -#    = event.title
      -#    %div.pull-right= event.date
      -#  .panel-body
      -#    .row
      -#      .col-md-8
      -#        = event.description
      -#      .col-md-2.col-md-offset-2
      -#        = image_tag event.image.url, :class => "img-responsive"

        -# %td= event.title
        -# %td= event.description
        -# %td= event.date
        -# %td= link_to 'Show', event
        -# %td= link_to 'Edit', edit_event_path(event)
        -# %td= link_to 'Destroy', event, :method => :delete, :data => { :confirm => 'Are you sure?' }

    %br

    = link_to 'New Event', new_event_path
