-# list of event
.panel.panel-info
  .panel-heading
    %h4 Events
  %ul.list-group
  - events.each do |event|
    %li.list-group-item= link_to event.title, :controller => "events", :action => "show", :id => event.id
